from django.db import models
from django.contrib.auth.models import User

# We'll use Django's built-in User model for now
# No custom model needed as Django's User model already supports email and password hashing
